define(['ui', 'map'], function(ui, map){
    console.log(map);

    //ui.toggleActiveAdd('download', 'download-page');

    //$(document).on('pageshow', '#saved-maps=page',  offlineMaps);
    //$(document).on('pageshow', '#save-map', saveMap);

    var offlineMaps = function(){
        console.log("jings");
    };

    var saveMap = function(){
        console.log("crivvens");
    };
});
